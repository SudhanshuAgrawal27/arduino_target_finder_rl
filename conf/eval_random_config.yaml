# Config for the standalone eval_random.py. Override checkpoint_dir on the
# command line to point at any checkpoint, e.g.:
#   python3 eval_random.py checkpoint_dir=trained_models/<run>/epoch_150
#
# `seed` is fixed (not left to change per-run) so every checkpoint is
# evaluated on the exact same sequence of problem instances -- that's what
# makes eval_stats comparable across checkpoints/epochs. Don't override it
# when comparing checkpoints; only override checkpoint_dir.

defaults:
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null

checkpoint_dir: trained_models/h64_l3_hist4_ep150_seed43/epoch_150
episodes: 50
seed: 123
