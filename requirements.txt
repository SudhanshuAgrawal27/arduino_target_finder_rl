# ============================================================
# Training environment requirements
# Python 3.12 | CUDA 12.4 | MX250 / A100 / H100
#
# NOTE: torch, torchvision, torchaudio are installed first in
#       the Dockerfile (before flash-attn) using the CUDA index.
#       flash-attn is installed with --no-build-isolation.
#       pip will skip both when it hits them here.
#
# Pinned as of July 2026 — transformers 5.x branch.
# ============================================================

--extra-index-url https://download.pytorch.org/whl/cu124

# --- Triton (kernel backend for flash-attn and torch.compile) ---
triton==3.0.0

# --- Core Hugging Face stack ---
# transformers 5.x requires: tokenizers >= 0.22, safetensors >= 0.8, hub >= 1.5
transformers==5.13.0
tokenizers==0.22.2
safetensors==0.8.0
datasets==5.0.0
pyarrow==21.0.0
evaluate==0.4.6
huggingface_hub==1.5.0

# --- Training orchestration ---
accelerate==1.14.0
peft==0.19.1
trl==1.7.0

# --- Quantization ---
bitsandbytes==0.49.2

# --- Distributed / large-scale training ---
deepspeed==0.19.2

# --- Scientific computing ---
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
einops==0.8.0

# --- Tokenization utilities ---
sentencepiece==0.2.0
tiktoken==0.8.0

# --- Experiment tracking ---
wandb==0.28.0

# --- GPU + system monitoring (Python) ---
gpustat==1.1.1

# --- Build acceleration ---
ninja==1.11.1.1

# --- Misc ---
tqdm==4.66.5

# --- Config management ---
hydra-core==1.3.4
omegaconf==2.3.1

# --- Testing ---
pytest==8.3.3
